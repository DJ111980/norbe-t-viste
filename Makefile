install:
	npm install

dev:
	npm run dev

dev-web:
	npm run dev:web

dev-api:
	npm run dev:api

format:
	npm run format

lint:
	npm run lint

test:
	npm run test

build:
	npm run build

check:
	npm run check

db-migrate:
	npm run db:migrate

db-migrate-remote:
	npm run db:migrate:remote

db-seed:
	npm run db:seed

db-schema:
	npm run db:schema

admin-create:
	npm run admin:create
